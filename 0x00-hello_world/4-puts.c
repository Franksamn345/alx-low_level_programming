#include <stdio.h>

/**
 * main - Entry to code
 *
 * Return: when the program returns 0 its a success
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
