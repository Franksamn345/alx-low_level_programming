#include <stdio.h>

/**
 * main - Entry to code
 *
 * Return: when the program returns 0 then program is successfull
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
